# buf.gen.yaml
version: v1
managed:
  enabled: true
plugins:
  - plugin: buf.build/protocolbuffers/dart:v21.1.2
    out: gen
    opt:
      # Enable the "grpc" option if you need gRPC stubs.
      - grpc